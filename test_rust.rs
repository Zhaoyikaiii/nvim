// Simple Rust test file
fn main() {
    let message = "Hello, rust-analyzer with 512MB memory optimization!";
    println!("{}", message);

    let numbers = vec![1, 2, 3, 4, 5];
    let sum: i32 = numbers.iter().sum();

    println!("Sum: {}", sum);
}